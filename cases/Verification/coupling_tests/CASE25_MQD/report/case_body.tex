\section{Verification purpose}

\textbf{Case ID:} \texttt{\CaseID}. This case verifies spatial moisture lookup, moisture damping, and extinction behavior in otherwise uniform fuel.  The verification question is whether the four regional spread rates meet their moisture-specific references, including negligible spread at the extinction condition.

\section{Mathematical formulation and numerical implementation}

Fuel moisture reduces reaction intensity through the model's moisture-damping term; at or above the fuel's moisture of extinction, sustainable spread approaches zero.  Only the one-hour moisture raster varies, so regional velocity differences should follow that damping response.

\section{Derivation of expected behavior}

For Fuel Model 3 the configured moisture values 6, 12, 18, and 25 percent correspond to reference rates \(1.511\), \(1.089\), \(0.801\), and \(0~\mathrm{m\,min^{-1}}\).  The rates must decrease monotonically as moisture rises.

\section{Verification metrics and acceptance criteria}

The maximum finite \texttt{vs} in each quadrant measures the local head response without averaging across moisture boundaries. Nonzero targets use 10 percent relative tolerance. The extinction quadrant uses an absolute \(0.05~\mathrm{m\,min^{-1}}\) limit because relative error is undefined at zero. All four components must pass.

\section{Simulation configuration and predicted outcome}

The grid uses uniform Fuel Model 3, flat terrain, zero wind, central ignition, and one-hour moisture quadrants of 6, 12, 18, and 25 percent. Predicted propagation slows monotonically and stops in the 25-percent quadrant.

\section{Actual simulation results}

The required evidence is generated only from case-local ELMFIRE outputs. The current postprocessing status is \textbf{\VerificationStatus}; \PassingMetricCount{} of \MetricCount{} required metric rows pass across \VariantCount{} variant(s). A missing output is reported as NOT RUN or NOT EVALUABLE and is not interpreted as verification evidence.


\begin{figure}[H]
  \centering
  \IfFileExists{../figures/whole_domain_result.pdf}{%
    \includegraphics[width=0.82\textwidth,height=0.58\textheight,keepaspectratio]{whole_domain_result.pdf}%
  }{\fbox{\parbox{0.85\textwidth}{No case-local whole-domain ELMFIRE raster is available.}}}
  \caption{Representative whole-domain case-local ELMFIRE output. The generated figure records its source path, coordinate reference system, and variant.}
\end{figure}


\begin{figure}[H]
  \centering
  \IfFileExists{../figures/verification_summary.pdf}{%
    \includegraphics[width=\textwidth,height=0.58\textheight,keepaspectratio]{verification_summary.pdf}%
  }{\fbox{\parbox{0.85\textwidth}{No case-local summary figure is available.}}}
  \caption{Case-local expected and measured verification metrics. A missing measured value denotes unavailable or incomplete ELMFIRE output.}
\end{figure}

\section{Calculated metrics and verification decision}

\begin{center}
\begin{tabular}{@{}llll@{}}
\toprule
Variant / metric & Expected & Calculated & Status \\
\midrule
\MetricRows
\bottomrule
\end{tabular}
\end{center}

The authoritative overall decision is \textbf{\VerificationStatus} and is generated by \texttt{scripts/postprocess.py} from \texttt{outputs/metrics.json}; it is not recomputed in this report.

\section{Technical interpretation}

Nonmonotone behavior suggests moisture raster or damping errors; significant spread in the extinction quadrant is a direct failure. No scientific conclusion should be drawn when the status is NOT RUN or NOT EVALUABLE.

\section{Scope and limitations}

Only one fuel model and the one-hour dead-fuel moisture field are varied. This case provides implementation-verification evidence only.
